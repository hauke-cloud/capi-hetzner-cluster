{{ if .Values.bootstrap.cilium.enabled }}
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: {{ include "capi-hetzner-cluster.name" . }}-cilium
spec:
  type: {{ .Values.bootstrap.cilium.repoOverrides.type | default "default" }}
  interval: {{ .Values.bootstrap.cilium.repoOverrides.interval | default "5m0s" }}
  url: {{ .Values.bootstrap.cilium.repoOverrides.url | default "https://helm.cilium.io/" }}
---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: {{ include "capi-hetzner-cluster.name" . }}-cilium
spec:
  chart:
    spec:
      chart: {{ .Values.bootstrap.cilium.chartOverrides.name | default "cilium" }}
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: {{ include "capi-hetzner-cluster.name" . }}-cilium
      version: {{ .Values.bootstrap.cilium.version | default "*" }}
  interval: {{ .Values.bootstrap.cilium.chartOverrides.interval | default "5m0s" }}
  targetNamespace: {{ .Values.bootstrap.cilium.chartOverrides.targetNamespace | default "cilium" }}
  storageNamespace: {{ .Values.bootstrap.cilium.chartOverrides.targetNamespace | default "cilium" }}
  kubeConfig:
    secretRef:
      name: {{ .Values.bootstrap.cilium.chartOverrides.kubeConfig.secretRef.name | default (printf "%s-kubeconfig" (include "capi-hetzner-cluster.name" .)) }}
      key: {{ .Values.bootstrap.cilium.chartOverrides.kubeConfig.secretRef.key | default "value" }}
{{- end }}
